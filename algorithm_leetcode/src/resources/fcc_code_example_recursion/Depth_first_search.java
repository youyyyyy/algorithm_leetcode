/*
 * Code example from freeCodeCamp.org
 * url = https://www.youtube.com/watch?v=IJDJ0kBx2LM
 * */

package resources.fcc_code_example_recursion;

import java.util.Set;

public class Depth_first_search {
/*    boolean depthFirstSearch(Node node, Set<Node> visited, int goal) {
        // set is to prevent visiting the same node more than once

        // base case
        if (node == null) return false;

        if (node.val == goal) {
            return true;
        }

        for (Node neighbour : node.getNeighbours()) {
            if (visited.contains(neighbour)) continue;
            visited.add(neighbour);
            boolean isFound = depthFirstSearch(neighbour, visited, goal);
            if(isFound) return true;
        }
        return false;

    }*/
}
